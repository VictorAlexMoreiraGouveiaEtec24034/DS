""" Sistema de interação via cmd"""

# importações de libs e arquivos
from Terminal import *
import sys

class terminal:
    def __init__(self):
        self.cmd = []
    
    def View(self):
        self.cmd.clear()
        for i in range(5):
            return input("<Terminal/Home> ")
            self.cmd.append(valor)
    
    